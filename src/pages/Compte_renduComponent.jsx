import React from 'react';
import Typography from '@mui/material/Typography';

const Compte_renduComponent = () => {
    return (
        <div className="content-area">
            <Typography variant="h2">Compte rendu</Typography>
        </div>
    );
};

export default Compte_renduComponent;

